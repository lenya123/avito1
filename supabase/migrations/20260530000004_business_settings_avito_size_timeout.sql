-- ============================================================================
-- ТЗ Авито-заказы (§4.2 п.7, §10 Q8): таймаут ожидания ответа покупателя
-- про размер. По дефолту 12 часов (рабочий день).
--
-- Если за это окно покупатель не ответил → status awaiting_size остаётся,
-- мини-AI повторно не пишет, эскалация директору, заказ может уйти в cancelled
-- (если send_by уже близок).
-- ============================================================================

ALTER TABLE public.business_settings
  ADD COLUMN IF NOT EXISTS avito_size_request_timeout_hours INTEGER NOT NULL DEFAULT 12;
