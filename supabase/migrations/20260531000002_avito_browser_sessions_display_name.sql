-- ============================================================================
-- Кастомное имя Avito-аккаунта (вводится владельцем при подключении).
--
-- Используется в UI вместо shop_name (тот приходит из API и может быть
-- пустым/невнятным). Если оставлено пустым — fallback на shop_name → логин.
-- ============================================================================

ALTER TABLE public.avito_browser_sessions
  ADD COLUMN IF NOT EXISTS display_name TEXT;
