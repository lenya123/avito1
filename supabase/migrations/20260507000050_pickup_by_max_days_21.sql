-- ============================================================================
-- Окно даты «забрать возврат с ПВЗ» — дефолт 14 → 21 (3 недели).
-- ============================================================================
-- Решение walkthrough'а 2026-05-07: 3 недели — более реалистичное окно
-- для дропшипперов чтобы успеть забрать возврат с ПВЗ.

ALTER TABLE public.business_settings
  ALTER COLUMN pickup_by_max_days SET DEFAULT 21;
